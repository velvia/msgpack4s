language: scala
env:
  global:
    # _JAVA_OPTIONS="-Xmx1500m -XX:MaxPermSize=512m -Dakka.test.timefactor=3"
scala:
   - 2.11.7
   - 2.12.0
jdk:
  - oraclejdk8

before_script:
  - sudo chmod +x /usr/local/bin/sbt
